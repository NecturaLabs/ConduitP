{
  "name": "@conduit/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "conduit": "./dist/install-hooks.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.7.0"
  }
}
